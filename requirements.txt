# PNN-inspired reliability-gated consolidation for continual learning.
# Pinned to the exact environment used for the Neural Networks submission package
# (verified working: Python 3.13.11, CUDA 13.0, NVIDIA RTX 2050 4 GB; 2026-06-19).
#
# Torch/torchvision are CUDA builds. Install them FIRST from the matching PyTorch
# index, then `pip install -r requirements.txt` for the rest. Example (the build
# this package was validated on):
#   pip install torch==2.12.0 torchvision==0.27.0 --index-url https://download.pytorch.org/whl/cu130
# (CPU-only is fine for the smoke test: drop the index-url.)

torch==2.12.0
torchvision==0.27.0
numpy==2.4.6
scipy==1.17.1
scikit-learn==1.9.0
pandas==3.0.3
matplotlib==3.11.0
seaborn==0.13.2
pyyaml==6.0.3
pytest==9.0.3

# --- External continual-learning framework (OPTIONAL) ---
# The submission pipeline does NOT depend on Mammoth; all baselines (ER, DER++,
# EWC, SI) are implemented natively in baselines/ and run through the shared
# ContinualTrainer. Mammoth was used only for an earlier sanity comparison.
# To reproduce the Mammoth "Stage-0" runs, see patches/README.md (pinned commit
# aimagelab/mammoth @ e75a491c69fd729edeb01431afb753d9157d9a81 + local patch) and
#   pip install -r patches/requirements-mammoth-stage0.txt
